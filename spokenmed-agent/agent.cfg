# SpokenMED Agente — configuração
# Edite as linhas abaixo. Salve como agent.cfg (sem o .example).

WORKER_API_KEY=COLE_AQUI_SUA_API_KEY_DO_WORKER
FIORILLI_USERNAME=seu_usuario
FIORILLI_PASSWORD=sua_senha

# Índice da unidade no popup que aparece logo após o login no Ambulatório.
# 0 = primeira da lista, 1 = segunda, etc. Default 4 (Teresópolis no HAR).
FIORILLI_UNIDADE_INDEX=4
